#include <stdio.h>
void main()
 {
   int num, i, sum=0;
   printf("input any number:");
   scanf("%d",&num); 
   printf("1+");
   for(i=2;i<n;i++)
    printf("1/%d+",i); 
   for(i=1;i<n;i++)
    {
      sum+=i; 
      printf("1/%d",num); 
    }
   printf("\nsum=1/%d",sum+1/num);
}   
